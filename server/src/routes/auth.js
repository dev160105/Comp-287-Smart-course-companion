// src/routes/auth.js
// Phase 2: Authentication routes (Login, Signup, Refresh Token)

const authRoutes = /* express.Router() */ {
  /*
  POST /api/auth/signup
  - Create new user account
  - Body: { firstName, lastName, email, password, role }
  - Returns: { user, token }

  POST /api/auth/login
  - Authenticate user
  - Body: { email, password }
  - Returns: { user, token }

  POST /api/auth/refresh
  - Refresh JWT token
  - Body: { refreshToken }
  - Returns: { token }

  POST /api/auth/logout
  - Invalidate token
  */
};

export default authRoutes;
